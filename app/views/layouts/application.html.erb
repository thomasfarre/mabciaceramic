<!DOCTYPE html>
<html>
  <head>
    <title>Mabciaceramic</title>
    <meta charset="utf-8">
    <!-- Hotjar Tracking Code for https://www.mabciaceramic.com -->
    <script>
        (function(h,o,t,j,a,r){
            h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
            h._hjSettings={hjid:2217529,hjsv:6};
            a=o.getElementsByTagName('head')[0];
            r=o.createElement('script');r.async=1;
            r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
            a.appendChild(r);
        })(window,document,'https://static.hotjar.com/c/hotjar-','.js?sv=');
    </script>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

<!--     it is disable because of style for administrate -->
    <%#= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  </head>

  <body data-controller="gdpr-popup">

    <% if !current_page?(controller: 'devise/sessions', action: 'new') && !current_page?(controller: 'registrations', action: 'new') && !current_page?(controller: 'registrations', action: 'edit')%>
      <%= render "shared/navbar" unless @disable_nav %>
    <% end %>

    <%#= render "shared/gdpr_popup" %>

    <%= yield %>

    <% if !current_page?(controller: 'devise/sessions', action: 'new') && !current_page?(controller: 'registrations', action: 'new') && !current_page?(controller: 'registrations', action: 'edit') %>
      <%= render "shared/footer" unless @disable_footer %>
    <% end %>
  </body>
</html>
