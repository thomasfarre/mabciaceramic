<header data-controller="menu">
  <div class="flex items-center justify-between bg-gray-100 py-1 px-2 shadow">
    <div>
      <%= link_to root_path do %>
        <%= image_tag "Logo.png" %>
      <% end %>
    </div>

    <div>
      <h2 class="text-gray-600 font-bold text-2xl">MabciaCeramic</h2>
    </div>

    <div  class="flex items-center outline-none">
      <button data-action="click->menu#toggle" id="options-menu" class="text-gray-600 focus:outline-none hover:text-gray-400 focus:text-gray-400">
        <svg class="w-8 z-40" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
      </button>

    </div>
  </div>
  <div class="sticky">
    <%= current_user.nil? ? (render 'shared/menu_before_login') : (render 'shared/menu_after_login') %>
  </div>
</header>
