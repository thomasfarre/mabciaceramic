
      <h1>Order #<%= @order.id %> <small><span class="badge badge-primary"><%= @order.status %></span></small></h1>
      <p>Estimated delivery in <strong>2 business days</strong></p>
      <% @order.cart.cart_items.each do |cart_item| %>
        <li><strong>ITEM</strong> <%= cart_item.item.title %> <strong> PRICE</strong> <%= humanized_money_with_symbol cart_item.item.price %></li>
      <% end %>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
        </li>
      </ul>
    </div>
  </div>
</div>

<%= link_to "Go back", cart_path(@cart) %>
